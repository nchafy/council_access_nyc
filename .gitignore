# Fetched upstream payloads — large, re-downloadable, and a moving target.
# Everything here is regenerated by the ETL; never commit it.
etl/raw/
data/raw/
data/interim/
*.pdf.cache

# Build output
web/data/
site/
dist/
.vite/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Node (mapshaper is the only devDependency)
node_modules/

# Editor / OS
.DS_Store
*.swp
.idea/
